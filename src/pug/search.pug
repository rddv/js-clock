doctype html
html(lang="en")
    include components/head
    head
        +head
    body.body
        .wrapper
            .search-box
                form.search__form
                    input.search__input(type='text', placeholder='Country...')
                ul.search__list
                    li.search__item
                        span.search__city Find the capital
        script(src='js/main.js')